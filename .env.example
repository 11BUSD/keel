# Keel prototype — no secrets required.
# The demo runs entirely on simulated in-browser data (ADR-0004).
# This file documents the (empty) config surface per Contract §F1.

# Example of how future config would be added (do NOT put real values in git):
# NEXT_PUBLIC_DEMO_SEED=1337
